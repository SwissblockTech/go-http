
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app: http-client

commonAnnotations:
  version: v1.0.0

images:
  - name: bygui86/http-client
    newTag: v1.0.0
